# Config: 7900 XTX — q8_0 KV, 32k context (no draft)

**Card:** AMD Radeon RX 7900 XTX (24 GB) · **Backend:** Vulkan

| Knob | Value | Why |
|------|-------|-----|
| Model | Qwen3.8-27B-Q4_K_M.gguf (≈18.9 GB) | Leaves ~5 GB for KV + activations |
| ctx | 32768 | Initial ceiling guess for 24 GB; first run must establish the real max |
| KV | q8_0 / q8_0 | Halves KV vs f16; required at ≥32k on this card (f16 only ≤16k) |
| n-gpu-layers | 999 | Full offload |
| flash-attn | auto (resolves to on) | |
| MTP draft | none | No headroom for a draft at 32k until measured otherwise |

Launcher: [`examples/7900xtx-server.sh`](../../examples/7900xtx-server.sh) (defaults match this config).

**Status:** expected, unverified — first hardware run pending. Also run an 8B-class model at 64k+ to map the card's comfortable regime.
